import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'
import tw from 'twrnc';

const Cart = () => {
  return (
    <ScrollView contentInsetAdjustmentBehavior="automatic" style={tw`pt-0`}>
    </ScrollView>
  )
}

export default Cart

const styles = StyleSheet.create({})